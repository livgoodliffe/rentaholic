<div class="d-flex flex-wrap justify-content-center">
  <% Item::CATEGORIES.each do |category| %>

    <%= link_to root_path(controller: "items", query: "technology") do %>

      <div class="category-card-border">
        <div class="category-card">
            <h3><%= category %></h3>
        </div>
      </div>

    <% end %>
  <% end %>
</div>
